---
name: harness-parallel-execution
description: Claude Codeで各ジャンルのハーネス作業を並列実行するときに使う共通正本アダプター。「並列でやって」「同時に走らせて」「一気に終わらせて」「最短で」「サブエージェントを立てて」と言われたとき、または同種の作業が多数並んでいるときに必ず使う。
---

# Claude Code用アダプター

並列実行に関する作業を始める前に、`../../../.agents/skills/harness-parallel-execution/SKILL.md`を最後まで読み、共通正本として厳守する。実測した並列上限、並列にしてよい工程と直列必須の工程、共有状態ファイルの一覧はすべてそちらにある。推測で並列化しない。
